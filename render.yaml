services:
  - type: web
    name: classlinker
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/classlinker-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
